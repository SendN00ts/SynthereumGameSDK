import { GameWorker, GameFunction } from "@virtuals-protocol/game";
export declare const DEFAULT_API_KEY = "UP-17f415babba7482cb4b446a1";
export declare const DEFAULT_BASE_API_URL = "https://api.together.xyz/v1/images/generations";
interface IImageGenPluginOptions {
    id?: string;
    name?: string;
    description?: string;
    apiClientConfig: {
        apiKey?: string;
        baseApiUrl?: string;
    };
}
declare class ImageGenPlugin {
    private id;
    private name;
    private description;
    private apiKey;
    private baseApiUrl;
    constructor(options: IImageGenPluginOptions);
    getWorker(data?: {
        functions?: GameFunction<any>[];
        getEnvironment?: () => Promise<Record<string, any>>;
    }): GameWorker;
    get generateImage(): GameFunction<[{
        readonly name: "prompt";
        readonly description: "The prompt for image generation model. Example: A dog in the park";
        readonly type: "string";
    }, {
        readonly name: "width";
        readonly description: "Width of generated image, up to 1440 px. Default should be 1024 unless other sizes specifically needed.";
        readonly type: "number";
        readonly default: 1024;
    }, {
        readonly name: "height";
        readonly description: "Height of generated image, up to 1440 px. Default should be 1024 unless other sizes specifically needed.";
        readonly type: "number";
        readonly default: 1024;
    }]>;
}
export default ImageGenPlugin;
